

/*namespace ErsterProjekt
{
    internal class WhileSchleife
    {
        static void Main()
        {*/
            /*
            Random random = new Random();
            int current = random.Next(1, 11);

            do
            {
                current = random.Next(1, 11);
                if (current >= 8) continue;
                Console.WriteLine(current);
            } while (current != 7);*/
            /*
                        while (current >= 5)
                        {
                            Console.WriteLine(current);
                             current = random.Next(1, 11);

                        }
                        Console.WriteLine($"Last number: {current}");*/


            /*   int summe = 0; //*mit while schleife*//*
               int gesamtsumme;
               string resultat = "";

               int zaehler = 1;

               while (zaehler <= 30)
               {

                   if (zaehler == 1)
                       resultat = "1";
                   resultat = resultat + "+" + zaehler;


                   summe = summe + zaehler;
                   zaehler++;

                  *//* gesamtsumme = summe * 2; *//*innerhalb der schleife*//*

                   Console.WriteLine($"die resultat ist:{resultat}");
                   Console.WriteLine($"die summe ist:{summe}");
                   Console.WriteLine($"die gesamtsumme ist:{gesamtsumme}");*//*
               }

               gesamtsumme = summe * 2;

               Console.WriteLine($"die resultat ist:{resultat}");
               Console.WriteLine($"die summe ist:{summe}");
               Console.WriteLine($"die gesamtsumme ist:{gesamtsumme}"); */
            /*              int held = 10;
                            int monster = 10;
                            Random random = new Random();

                    do
                    {
                        int angriffeheld = random.Next(1, 11);
                        monster -= angriffeheld;

                        Console.WriteLine($"Held greift an mit {angriffeheld}, Monster hat jetzt {monster} HP");
                        if (monster <= 0)
                            break;
                        int angriffemonster = random.Next(1, 11);
                        held -= angriffemonster;
                        Console.WriteLine($"Monster greift an mit {angriffemonster}, Held hat jetzt {held} HP");
                    } while (held > 0 && monster > 0);



                        if (held > 0) 
                                Console.WriteLine("Der Held hat gewonnen!"); 
                            else
                                Console.WriteLine("Das Monster hat gewonnen!");*/
            /*
                        string? readResult;
                        Console.WriteLine("Enter a string:");
                        while (readResult == null)
                        {
                            readResult = Console.ReadLine();
                        }*/


            /*    while (bedingung)
                {
                    // auszuführender Code
                }



                do
                {
                    // auszuführender Code
                } while (bedingung)
    */

            /*Uebung 1:
             * Einmal while und einmal do-while! Am ende beschreiben: Was war besser? Wieso?
            Wir moechten ein kleines program schreiben wo der benutzer
            2 Zahlen eingeben soll. Wir multiplizieren diese 2 Zahlen dann miteinander.
            Dann geben wir das Resultat aus.
            Nachdem wir das Resultat ausgegeben haben, sollen wir den benutzer nochmal auffordern 2 Zahlen einzugeben.
            Dies soll so lange erfolgen bis der Benutzer das wort quit eingibt.
            Hinweis: Wir haben somit entweder eine Zahl oder einen String.
            Dies bedeutet erstmal ueberpruefen mit tryParse ob die konvertierung erfolgreich war.
            Wenn nicht, dann ob der String = quit ist. Ansonsten verstehen die eingabe nicht.
            Und die schleife startet neu.

            string eingabe = Console.ReadLine();
  false          int.TryParse(eingabe,out = 0)
            Bspw:
            Console:

            Wilkommen in unsere kleine Konsolen Applikation!
            Geben Sie bitte eine Zahl ein die wir mit eine 2te Zahl multiplizieren werden:
User:       3
            Danke! Geben Sie mir jetzt bitte die 2te Zahl:
User:       5
            Super! Das resultat von 3 * 5 = 15!
            ---Schleife durchgang 1 fertig---
            Geben Sie bitte eine Zahl ein die wir mit eine 2te Zahl multiplizieren werden:
User:       asdfasdf
            Uppsala! Das ist keine Zahl und wir stoppen nur wenn Sie 'quit' eingeben!
            ---Schleife durchgang 2 fertig---
            Geben Sie bitte eine Zahl ein die wir mit eine 2te Zahl multiplizieren werden:
User:       quit
            Bis zum naechsten mal!
            *Programm Vorbei*
            *//*
            Console.WriteLine(" Wilkommen in unsere kleine Konsolen Applikation! ");


            string? eingabe1 = Console.ReadLine();*/
           
/*


        } 
    } 
}*/
    

