@page
@model BiboApp.Pages.ListBooksModel

<h1>Alle Bücher</h1>

<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Titel</th>
            <th>Autor</th>
            <th>Genre</th>
            <th>Ausgeliehen</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var buch in Model.Buecher)
        {
            <tr>
                <td>@buch.Id</td>
                <td>
                    <a asp-page="/ShowBook" asp-route-id="@buch.Id">
                        @buch.Titel
                    </a>
                </td>
                <td>@buch.Autor</td>
                <td>@buch.GenreNavigation?.Bezeichnung</td>
                <td>@(buch.IsAusgeliehen ? "Ja" : "Nein")</td>
            </tr>
        }
    </tbody>
</table>